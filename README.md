# music-convert-util
The CLI Tool for converting music format easilly without doing ffmpeg stuff
## How to build
setup repo
```shell
git clone (myrepo)
```
then
```shell
chmod +x build.sh
./build.sh
```
